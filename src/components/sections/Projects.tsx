import { ArrowUpRight, Database, GitBranch, LineChart } from 'lucide-react';
const projects=[{icon:Database,code:'SYS_01',title:'HPCC ETL Architecture',text:'Extraction, transformation, and loading workflows built for source-to-target data reliability.',tags:['HPCC ECL','Talend','Datawarehouse']},{icon:GitBranch,code:'SYS_02',title:'Incremental Load Engine',text:'Partition-aware MySQL pipelines with staging tables, validation, and incremental load strategies.',tags:['Python','MySQL','SQL']},{icon:LineChart,code:'SYS_03',title:'Recovery & Insight Layer',text:'Error handling that resumes processing from failure, plus Python statistical analysis for edge cases.',tags:['Django','PySpark','Big Data']}];
export default function Projects(){return <section id="projects" className="section projects-section"><div className="section-kicker">04 / SELECTED SYSTEMS</div><div className="section-heading"><h2>Work that<br/><em>holds up.</em></h2><div className="section-copy">Engineering work translated from the résumé into a clear systems archive.</div></div><div className="project-grid">{projects.map(({icon:Icon,...p})=><article className="project-card" key={p.code}><div className="project-top"><span>{p.code}</span><Icon size={22}/></div><h3>{p.title}</h3><p>{p.text}</p><div className="project-tags">{p.tags.map(tag=><span key={tag}>{tag}</span>)}</div><a href="mailto:official.yogesh.mane@gmail.com?subject=Project%20discussion" aria-label={`Discuss ${p.title}`}><ArrowUpRight size={18}/></a></article>)}</div></section>}
