'use client';
import { Menu, X } from 'lucide-react';
import { useEffect, useState } from 'react';
const links=[['about','About'],['experience','Experience'],['projects','Projects'],['skills','Skills'],['certifications','Credentials'],['contact','Contact']];
export default function Navbar(){const[open,setOpen]=useState(false);const[active,setActive]=useState('top');useEffect(()=>{const ids=['top',...links.map(([id])=>id)];const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)setActive(entry.target.id)}),{rootMargin:'-35% 0px -55%'});ids.forEach(id=>{const el=document.getElementById(id);if(el)observer.observe(el)});return()=>observer.disconnect()},[]);return <header className="site-nav"><a href="#top" className="nav-brand" data-magnetic>YOGESH<span>.</span></a><nav className="desktop-nav">{links.map(([id,label])=><a className={active===id?'active':''} key={id} href={`#${id}`} data-magnetic>{label}</a>)}</nav><a className="nav-status" href="mailto:official.yogesh.mane@gmail.com"><i/> Say Hello</a><button className="menu-trigger" onClick={()=>setOpen(!open)} aria-label="Toggle navigation">{open?<X/>:<Menu/>}</button>{open&&<nav className="mobile-nav">{links.map(([id,label])=><a key={id} href={`#${id}`} onClick={()=>setOpen(false)}>{label}</a>)}</nav>}</header>}
